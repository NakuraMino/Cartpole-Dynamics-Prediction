CartpoleNet3.pth:
Trained for about 12k iterations so far. Uses an initial Adam optimizer with a learning rate of 0.01 (First 5K iterations). Saw that it performed poorly, so trained for about another 7K iterations. I still see that it performs poorly, so I'll train it again tomorrow. 
